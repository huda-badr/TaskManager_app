// API keys configuration
// Replace with your actual API keys in production

// Wit.ai API token for speech-to-text (free)
export const WIT_AI_TOKEN = 'PJB3GC27WHH2GXAGAKQ3WRATEMEXERV7';  // Wit.ai token for speech recognition

// Default export for the API keys (helps resolve linter warning about missing default export)
const apiKeys = {
  WIT_AI_TOKEN
};

export default apiKeys;